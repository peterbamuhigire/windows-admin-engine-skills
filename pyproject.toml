[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "windows-admin-engine"
version = "0.1.0"
description = "Catalogue, routing, schema, and command-line support for Windows Administration Skills Engine"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Peter Bamuhigire"}]
dependencies = []

[project.scripts]
windows-admin = "windows_admin.cli:main"

[tool.setuptools]
package-dir = {"" = "python"}

[tool.setuptools.packages.find]
where = ["python"]
